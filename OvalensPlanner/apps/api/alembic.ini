# Alembic Configuration
# DATABASE_URL is read from environment (see alembic/env.py).
# For Supabase, set DATABASE_URL to your Postgres connection string.

[alembic]
script_location = alembic
sqlalchemy.url = driver://user:pass@localhost/dbname
